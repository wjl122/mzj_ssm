<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mzj.dao.NewdsDao" >
  
  <select id="queryAll" resultType="NewdsModel">
    SELECT  *  FROM  newds order by id desc  
    LIMIT #{offset}, #{limit}
  </select>

</mapper>