<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mzj.dao.FriendLinksDao" >
  
  <select id="queryAll" resultType="FriendLinksModel">
    SELECT  *  FROM  friendlinks order by id desc  
    LIMIT #{offset}, #{limit}
  </select>

</mapper>